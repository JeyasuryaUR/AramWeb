/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com', '*.googleusercontent.com'],
  },
};

export default nextConfig;
